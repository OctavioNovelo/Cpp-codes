#include <iostream>

using namespace std;

int main(){

    long long live;
    long long enemy;

    cin >> live >> enemy;

    while(live <= enemy){
        live = live * 2;
        
    }
    cout << live;
}